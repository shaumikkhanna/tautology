import { Scorekeeper } from "./Scorekeeper";

export const metadata = {
	title: "Scorekeeper | toomuchmaths",
	description:
		"A quick scorekeeper with a random player picker and die for tabletop games.",
};

export default function ScorekeeperPage() {
	return <Scorekeeper />;
}
